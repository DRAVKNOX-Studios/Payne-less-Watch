<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/colorBackground">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@android:color/transparent"
        android:elevation="0dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/toolbar_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/app_name"
                android:textSize="20sp"
                android:textStyle="bold"
                android:paddingStart="8dp" />

            <ImageButton
                android:id="@+id/btn_settings"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_settings"
                android:contentDescription="@string/settings" />
        </LinearLayout>
    </androidx.appcompat.widget.Toolbar>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- Shared FAB — used by Alarm and Timer tabs. One view instead of many. -->
        <TextView
            android:id="@+id/shared_fab"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_gravity="bottom|end"
            android:layout_margin="24dp"
            android:background="@drawable/bg_fab"
            android:elevation="6dp"
            android:gravity="center"
            android:text="+"
            android:textSize="28sp"
            android:visibility="gone" />

        <!-- Shared Undo bar — used by Alarm and Timer tabs for deletions. -->
        <LinearLayout
            android:id="@+id/shared_undo_bar"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_gravity="bottom"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="96dp"
            android:background="@drawable/bg_card"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="16dp"
            android:paddingEnd="8dp"
            android:visibility="gone"
            android:elevation="6dp">

            <TextView
                android:id="@+id/shared_undo_message"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Item deleted"
                android:textColor="@color/colorTextPrimary"
                android:textSize="14sp" />

            <TextView
                android:id="@+id/shared_undo_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:padding="8dp"
                android:text="UNDO"
                android:textColor="@color/colorAccent"
                android:textSize="14sp"
                android:textStyle="bold" />
        </LinearLayout>
    </FrameLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:background="#1A000000" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:orientation="horizontal"
        android:background="@android:color/transparent">

        <TextView
            android:id="@+id/tab_alarm"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:text="Alarm"
            app:drawableTopCompat="@drawable/ic_alarm"
            app:drawableTint="@drawable/tab_text_color"
            android:paddingTop="8dp"
            android:textColor="@drawable/tab_text_color"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tab_timer"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:text="Timer"
            app:drawableTopCompat="@drawable/ic_timer"
            app:drawableTint="@drawable/tab_text_color"
            android:paddingTop="8dp"
            android:textColor="@drawable/tab_text_color"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tab_stopwatch"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:text="Stopwatch"
            app:drawableTopCompat="@drawable/ic_stopwatch"
            app:drawableTint="@drawable/tab_text_color"
            android:paddingTop="8dp"
            android:textColor="@drawable/tab_text_color"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tab_world_clock"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:text="World"
            app:drawableTopCompat="@drawable/ic_world_clock"
            app:drawableTint="@drawable/tab_text_color"
            android:paddingTop="8dp"
            android:textColor="@drawable/tab_text_color"
            android:textStyle="bold" />

    </LinearLayout>
</LinearLayout>
